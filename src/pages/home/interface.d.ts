export type BaseType = {
    data: string,
    isActive: boolean,
    isRepeat: boolean // 是否重复
}

export type dataType = {
    data: string | number,
    innerPosition: Array,
    outerPosition: Array
}